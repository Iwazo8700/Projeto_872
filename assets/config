# Altura e largura da tela
screen_width = 880
screen_height = 880

# Número de linhas e colunas no mapa
lines = 40
columns = 20

# Tempo entre inicial cada queda automática do bloco
speed = 500

# Distância x e y das bordas da janela para as bordas do tabuleiro
shift_x = 20
shift_y = 20

# Tempo de delay entre cada iteração do loop principal
delay = 20

# Tempo mínimo entre dois clicks do teclado
keyboard_time = 90

# Número de linhas para que a velocidade de decida aumente
number_dec = 10

# Decrécimo no tempo para cada aumento de velocidade
decrease = 50

# Altura e largura de cada bloco
#block_size_x = 20
#block_size_y = 20

# Número de linhas que o bloco desce a cada clique
num_down = 2

# Grossura das linhas do mapa
thickness = 4

# Cores das linhas do mapa
line_r = 255
line_g = 255
line_b = 255

# Cores do bloco
block_r = 0
block_g = 0
block_b = 255

# Define se as peças dos jogadores devem colidir entre si
normal = false

host_ip = 127.0.0.1
